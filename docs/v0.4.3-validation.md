# 0.4.3 验收记录

日期：2026-09-24。沿用系统 Python 3.12.2 与 PyMuPDF／PyMuPDF4LLM／Layout 1.28.2，不新增运行依赖。

## 目录误判修正

版面模型会把目录的标题、点线和页码识别为表格，也可能拆成多个候选框。现在按可见字形基线重组原生文字，核对连续目录项、点线、右侧页码及顺序；只有完全位于已核实目录块内的候选框才被排除。无标题续目录需要多级章节号证据。

原始模型框保留在 `visual_table_regions`，排除依据写入 `non_table_regions`。同页其他表格继续检查；目录块与图片、明显线框相交，或缺少足够证据时仍保守处理。该规则不会删除 Markdown 中的目录，也不代表所有目录和表格都能准确分类。

## 回归测试

使用 `py -3.12 -m pytest -q -o addopts='' -p no:cacheprovider --basetemp=_agent/tmp/20260924-v043/tests` 验证：**166 passed**。

新增 19 项目录回归，覆盖中英文标题、无标题续目录、乱序片段、罗马数字页码、旋转页面、同页合并／图片表格、目录内小图、混合区域、隐藏／页外文字和标题位置。两项测试直接使用固定版 Layout 模型，验证拆分候选框的真实行为。测试禁止网络连接，未读取真实 token 或调用云端 OCR。

## 原文件复测

对 `GBT-33349-2016《往复式内燃燃气电站系统通用技术条件》.pdf` 的 19 页重新执行本机 `mineru-ocr preflight`：

| 物理页 | 0.4.2 | 0.4.3 |
| --- | --- | --- |
| 2 | `uncertain_table_structure`，提前决定云端 | 识别 35 条目录项，无阻断原因 |
| 3 | 单独检查也误报；整份预检因第 2 页阻断而跳过 | 识别 12 条续目录项，无阻断原因 |
| 8 | 整份预检尚未到此页就已决定云端 | 正确检测 `complex_table`，`cloud_pages: [8]` |

原文件 SHA-256：`c6620e32926034d6fc823fa603026a3a6c53312dbcd1cb944b77d226562dd6cb`。整份仍建议云端，但触发依据变为实际合并单元格表格。本次仅离线预检，未修改原 PDF 或既有 Markdown。

## 本机发布检查

源码包、CLI、安装元数据和全局 skill 统一为 0.4.3。全局 skill 位于 `C:\Users\shehy\.agents\skills\mineru-ocr`，与仓库内六个技能文件逐一核对 SHA-256；技能格式校验通过。安装沿用既有 Python，不变更 OCR 配置或密钥。测试和构建临时目录清理，仅保留必要验证日志。
