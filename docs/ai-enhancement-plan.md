# AI 增强方案（已退役）

本文原为 0.2.x 的额外模型增强设计。0.4.0 已移除豆包客户端、enhance 命令、图片语义解释和 AI JSONL 生成；旧方案的具体实现可从 Git 历史查看，不再作为操作指南。

当前项目只生成高还原度、可追溯的 Markdown 素材包。视觉识别、语义提炼、分块、向量化和知识关系生成应在知识摄入或 RAG 前置处理中按具体目标实现，并保留对原文和图片的引用。转换阶段的图片审核仅用于判断独立图块是否无效，不把模型描述插入正文。

已有 AI 派生文件和本机旧配置不会自动删除。当前用法见 [README](../README_zh-CN.md)。
