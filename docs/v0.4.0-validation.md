# 0.4.0 验收记录

日期：2026-09-21。环境：Windows，已有 Python 3.12.2。未安装新依赖或创建运行环境，未重复调用云端 OCR。

## 回归与边界

`python -m pytest`：93 passed。覆盖既有拆分、恢复、合并、来源定位、表格转换和本版新增行为：

- 交付包迁移后不依赖 manifest，验证结果区分记录哈希与引用检查。
- 内部证据篡改、图片哈希变化、缺失图片、工作目录嵌套均被拒绝。
- 独立无效图块按输入与图片哈希、具体引用行排除，原件和快照保留。
- 参考式图片、HTML img 和重复出现位置正确处理；共享下载链接保留。
- 正文内嵌图、表格内图、代码中的图片示例、链接包裹的图片不被删除规则误处理。
- 同字节图片共用资源，相似但字节不同的图保持独立；多文档共享资源不覆盖原成果。
- 退役的增强命令在解析阶段拒绝，旧本机配置字段不泄露且不自动删除。

项目 skill 和已同步的全局 skill 均通过 skill-creator 的 quick_validate；CLI `--version` 返回 0.4.0。

## GB 6932—2015 扫描件

输入：`GB-6932-2015《家用燃气快速热水器》.pdf`，108 个物理页。

原文件 SHA-256：`e7cc0ab9449e8908b147629a36ede31bca1d972ce1ecc1e2f47472c564bf39c4`。

复用已有原始 OCR 包和维护的 [原页校勘文件](examples/gb6932-2015-wiki-review.json)，通过默认 generic/source/auto 流程生成，不读取 gas-std-wiki 规则。

| 检查项 | 结果 |
|---|---|
| 交付目录 | 1 份主 Markdown + images；无 manifest、审计或伴随报告 |
| 引用图像 | 131 个唯一文件：42 幅来源裁图和 89 个被引用的原页 |
| 原始独立图像清单 | 42 张，42 个字节哈希；本样本未新增删除决定 |
| 表格 | 56 张：8 张转换为 GFM，48 张保留 HTML |
| 表格原页核对链接 | 56 张均保留 |
| 显示公式核对链接 | 6 处均保留 |
| 定点文字校勘 | 复用原有 14 项记录 |
| 条款/标题定位 | 497 个；其中 2 个页码仍未可靠匹配 |
| 独立渲染 | marked GFM 渲染全部 56 张表；BeautifulSoup 对比 8 张转换表共 93 个单元格，一致 |
| 与 0.3 版正文比较 | 仅一行说明性 HTML 注释变更；正文、图题、表格、公式和图片引用保持一致 |
| 迁移检查 | 仅复制 MD 与 images 到新目录；无记录条件下 references 校验通过 |

抽查了图 1 的设备示意和图 9 的带尺寸接头图，图形、标注及公差可读。本次没有对全文作新的逐字人工验收，也未借重复或尺寸特征推断图片无效。删除行为由独立构造用例验证，不能据此声称本标准实际删除了无效图块。

完整日志、内部记录、渲染结果和 skill 旧版备份保存在本机 `_agent/tmp/v040/`，样本交付保存在 `outputs/v0.4.0-gb6932/`。这些本地材料由 Git 忽略，未纳入源码交付；可丢弃测试目录和迁移验证副本在验收后回收。

## 已知边界

检查结果证明转换约束和资源引用成立，不代表测得 OCR 准确率。未知页码、正文遗漏、复杂公式、原版粗体含义等仍需原件复核。已迁移素材包可以独立消费，但没有自动重绑定旧工作记录的命令；后续可用原始 OCR 包继续处理。
