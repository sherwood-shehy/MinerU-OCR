# Optional gas-std-wiki adapter

Use only when the user selects this consumer. Generic conversion does not require a Wiki project, its rules or its metadata vocabulary.

The current local project may be at `D:\Codex-home\projects\gas-std-wiki`; confirm the user-selected target. Read its current AGENTS.md, project guide, general usage guide, ingestion/visual/tag/update/review rules and source-page template. Do not embed host paths in portable content or duplicate maintained business rules here.

`--profile gas-std-wiki --target-project TARGET` fingerprints nine known rule files. Missing files are explicit errors. This is a read-only baseline, not automatic compliance, registration or ingestion.

Prepare a generic source edition with conservative tables and relative images. Deliver Markdown plus images; keep converter manifests and review reports internal. A downstream workflow may opt into provenance records or transform them to its own schema, but must not assume ordinary readers understand the converter's manifest.

When ingestion is separately requested, follow the target's current source directories, indexes, templates and review rules. Never infer approval, legal effect or acceptance from successful conversion. Visual semantics, cross-document synthesis, controlled tags and knowledge pages belong to that ingestion stage.
