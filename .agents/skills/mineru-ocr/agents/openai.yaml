interface:
  display_name: "MinerU OCR"
  short_description: "Local PDF preflight, cloud OCR and faithful portable Markdown"
  default_prompt: "Use $mineru-ocr to preflight this PDF, select local extraction or authorized cloud OCR, and produce faithful Markdown with relative images, conservative tables and source-page links; preserve review evidence outside delivery and report actual checks and gaps."
